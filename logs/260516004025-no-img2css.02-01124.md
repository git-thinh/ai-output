The user wants me to recreate a specific UI from an image using HTML and Tailwind CSS v4.

**1. Analyze the Image:**
*   **Subject:** A product display page showing a pair of dark blue trousers.
*   **Layout:**
    *   **Top Bar:**
        *   Left: A "swap" or "compare" icon (two arrows in a circle).
        *   Right: A heart icon (wishlist/favorite).
    *   **Main Content:**
        *   Center: The product image (`/product.png`).
        *   Background: Light gray (`#f3f4f6` or similar).
    *   **Bottom Navigation (Carousel Controls):**
        *   Left: A circular arrow pointing left.
        *   Right: A circular arrow pointing right.
        *   Bottom Center: Pagination dots (one filled, two empty).

**2. Determine Colors & Spacing:**
*   **Background:** Very light gray, almost off-white. Let's try `bg-gray-50`.
*   **Icons:** Thin black lines. `text-black` or `text-gray-900`.
*   **Product Image:** Dark blue trousers. I will use the placeholder `/product.png`.
*   **Pagination Dots:** Small circle, filled with black, others empty.

**3. Tailwind v4 Implementation Strategy:**
*   Use `<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>`.
*   Use Bootstrap Icons for the UI elements (arrows, heart, swap icon).
*   **Swap Icon:** The image shows two arrows curving around each other. Bootstrap Icons has `bi-arrow-left-right` but that's straight. `bi-arrow-repeat` is a loop. The specific icon looks like a "compare" or "swap" symbol. I'll use `bi-arrow-left-right` or construct it with SVG if needed, but the prompt says "If a specific icon is not found, substitute with the closest visual equivalent". Actually, looking closely at the top left, it's two arrows curving. It looks like `bi-arrow-left-right` might be close enough, or maybe `bi-arrows-left-right`. Wait, looking really closely at crop 1, it's two arrows facing opposite directions but curved. It looks like a "swap" icon. I'll use `bi-arrow-left-right` as the closest standard icon, or perhaps just simple arrows. Let's look for a specific "swap" icon. `bi-exchange` is vertical. `bi-arrows-left-right` is horizontal. The image shows two arrows curving. I will use `bi-arrow-left-right` as a placeholder or try to find a custom SVG path if I were coding in a real environment, but here I must use Bootstrap Icons. Let's look at the shape again. It looks like two arrows forming a circle. `bi-arrow-repeat` is one arrow. Let's stick to `bi-arrow-left-right` or just generic arrows if needed. Actually, looking at the very first crop, it looks like `bi-arrow-left-right`. Wait, no, it's two arrows curving. I will use `bi-arrow-left-right` as the best approximation for a "swap" action in this context without custom SVGs.
*   **Heart Icon:** `bi-heart`.
*   **Navigation Arrows:** `bi-arrow-left`, `bi-arrow-right`.
*   **Pagination Dots:** Simple divs with rounded-full.

**4. Refining the Layout (Atomic Precision):**
*   **Container:** Full height (`h-screen` or similar) to match the mobile app feel? The image has black bars at top and bottom, suggesting a mobile frame or a specific aspect ratio. I will create a centered container with `max-w-md mx-auto` to simulate a mobile view.
*   **Top Bar:** Flexbox, `justify-between`, `p-4`.
*   **Image Area:** Centered vertically and horizontally. `flex flex-col items-center justify-center`.
*   **Bottom Bar:** Fixed or absolute positioning? In the image, it looks like it's floating above the bottom black bar. I'll use `pb-20` padding on the body to simulate that space.

**5. Dark Mode:**
*   The prompt asks for Dark and Light modes.
*   **Light Mode:** Light gray background (`bg-gray-50`), black text/icons.
*   **Dark Mode:** Dark background (`bg-black` or `bg-zinc-900`), white text/icons.
*   I need to wrap the content in a `dark:` class or use the `data-theme` attribute if Tailwind v4 supports it natively (it does via CSS variables). I will use the `dark:` modifier on the main container for demonstration.

**6. Specific Details:**
*   **Pagination:** 3 dots total. First is filled (`bg-black`), others empty (`border border-black`).
*   **Arrows:** Thin stroke icons.

Let's write the code.
<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product View - Atomic Replica</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css"/>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        /* Custom scrollbar hiding if needed, though not visible in screenshot */
        ::-webkit-scrollbar {
            display: none;
        }
    </style>
</head>
<body class="bg-gray-50 text-black transition-colors duration-300 dark:bg-black dark:text-white min-h-screen flex flex-col items-center justify-center relative overflow-hidden">

    <!-- Top Navigation Bar -->
    <div class="w-full p-4 flex justify-between items-center z-10">
        <!-- Swap Icon -->
        <button class="p-2 rounded-full hover:bg-gray-200 dark:hover:bg-zinc-800 transition-colors">
            <i class="bi bi-arrow-left-right text-xl"></i>
        </button>
        
        <!-- Heart Icon -->
        <button class="p-2 rounded-full hover:bg-gray-200 dark:hover:bg-zinc-800 transition-colors">
            <i class="bi bi-heart text-xl"></i>
        </button>
    </div>

    <!-- Main Content Area -->
    <div class="flex flex-col items-center justify-center w-full max-w-md px-4 pb-20">
        
        <!-- Product Image -->
        <img src="/product.png" alt="Dark Blue Trousers" class="w-full h-auto object-contain drop-shadow-sm">

    </div>

    <!-- Bottom Navigation / Carousel Controls -->
    <div class="fixed bottom-8 left-1/2 transform -translate-x-1/2 w-[90%] max-w-[300px] flex flex-col items-center gap-4 z-20">
        
        <!-- Pagination Dots -->
        <div class="flex items-center gap-2">
            <div class="w-1.5 h-1.5 rounded-full bg-black dark:bg-white"></div>
            <div class="w-1.5 h-1.5 rounded-full border border-black dark:border-white"></div>
            <div class="w-1.5 h-1.5 rounded-full border border-black dark:border-white"></div>
        </div>

        <!-- Navigation Arrows -->
        <div class="flex justify-between w-full px-4">
            <button class="p-3 rounded-full border border-black/20 dark:border-white/20 hover:bg-gray-100 dark:hover:bg-zinc-900 transition-colors">
                <i class="bi bi-arrow-left text-lg"></i>
            </button>
            <button class="p-3 rounded-full border border-black/20 dark:border-white/20 hover:bg-gray-100 dark:hover:bg-zinc-900 transition-colors">
                <i class="bi bi-arrow-right text-lg"></i>
            </button>
        </div>

    </div>

</body>
</html>