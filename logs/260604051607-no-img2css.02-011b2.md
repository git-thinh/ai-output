The user wants an exact HTML/Tailwind CSS replica of a product image showing a pair of navy blue trousers.

**Visual Analysis:**
1.  **Layout:** A central product image with navigation controls around it.
2.  **Top Bar:**
    *   Left: Share/Exchange icon (looks like two arrows crossing).
    *   Right: Heart icon (wishlist/favorite).
3.  **Main Content:**
    *   Center: A pair of navy blue trousers. The background behind the trousers is a very light gray/blue (`#f0f2f5` or similar).
    *   Left Side: A "Previous" arrow button (circle with `<`).
    *   Right Side: A "Next" arrow button (circle with `>`).
4.  **Bottom Bar:**
    *   Center: Pagination dots. One filled, two empty.

**Technical Constraints & Strategy:**
*   **Tailwind v4:** Use the CDN script provided.
*   **No JS:** Pure HTML/CSS for interactivity (hover states only).
*   **Icons:** Bootstrap Icons (`bi-`).
    *   Share/Exchange: `bi-arrow-repeat` or `bi-share`? The image shows two arrows crossing. `bi-arrow-repeat` is closest, or maybe a custom SVG if needed, but the prompt says "substitute with closest visual equivalent". Let's use `bi-arrow-repeat` or `bi-share`. Actually, looking closely at the top left, it looks like a "share" icon (arrows pointing out) or "exchange". Let's use `bi-share` or `bi-arrow-repeat`. Wait, standard e-commerce icons often use `bi-share` for this. Let's stick to `bi-share` or `bi-arrow-repeat`.
    *   Heart: `bi-heart`.
    *   Arrows: `bi-chevron-left`, `bi-chevron-right`.
*   **Colors:**
    *   Background: Light gray (`bg-gray-100` or similar).
    *   Buttons: White with black borders.
    *   Pagination: Dark blue/black for active, gray for inactive.
*   **Image:** I need to use a placeholder image since I don't have the actual file. The prompt says `src="/product.png"`. I will use a placeholder service or just the path as requested, but to make it look decent, I'll try to find a generic trousers image URL or stick strictly to the instruction `src="/product.png"`. The prompt explicitly says: "All `<img>` tags must use `src="/product.png"`". I will follow this strictly.
*   **Dark Mode:** The design is very light. Dark mode will invert the background to dark gray/black and buttons to dark gray with light borders/text.

**Step-by-Step Implementation Plan:**
1.  **Skeleton:** Create the main container with `min-h-screen`, `flex`, `items-center`, `justify-center`.
2.  **Top Controls:** Flex row, space-between. Icons on left/right.
3.  **Main Image Area:** Relative positioning to hold the image and absolute positioning for side buttons (left/right).
4.  **Pagination:** Absolute bottom center or flex column below image. Let's look at the image again. The pagination is *below* the image, centered.
5.  **Styling Details:**
    *   Buttons: `rounded-full`, `border`, `hover:bg-gray-100`.
    *   Image: `max-w-full`, `object-contain`.
    *   Pagination: Flex row, gap, rounded-full background.

**Refining the "Share" Icon:** The icon at top left is two arrows crossing. `bi-arrow-repeat` fits well.
**Refining the "Heart" Icon:** Standard outline heart `bi-heart`.

**Dark Mode Logic:**
*   Light: `bg-gray-50`, text black, buttons white/border-gray-300.
*   Dark: `bg-gray-900`, text white, buttons dark-gray/border-gray-700.

Let's write the code.
```html
<!DOCTYPE html>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css"/>
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<div class="min-h-screen bg-gray-50 dark:bg-gray-900 flex flex-col items-center justify-center p-4 transition-colors duration-300">
    <!-- Top Navigation Bar -->
    <div class="w-full max-w-md flex justify-between items-center mb-6">
        <!-- Left Icon: Share/Exchange -->
        <button class="text-gray-800 dark:text-white hover:bg-gray-200 dark:hover:bg-gray-800 p-2 rounded-full transition-colors">
            <i class="bi bi-arrow-repeat text-xl"></i>
        </button>
        
        <!-- Right Icon: Heart -->
        <button class="text-gray-800 dark:text-white hover:bg-gray-200 dark:hover:bg-gray-800 p-2 rounded-full transition-colors">
            <i class="bi bi-heart text-xl"></i>
        </button>
    </div>

    <!-- Main Product Area -->
    <div class="relative w-full max-w-md aspect-[3/4] bg-white dark:bg-gray-800 shadow-sm rounded-lg overflow-hidden flex items-center justify-center">
        <!-- Left Navigation Button -->
        <button class="absolute left-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full border border-gray-300 dark:border-gray-600 flex items-center justify-center text-gray-800 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors z-10">
            <i class="bi bi-chevron-left text-lg"></i>
        </button>

        <!-- Right Navigation Button -->
        <button class="absolute right-4 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full border border-gray-300 dark:border-gray-600 flex items-center justify-center text-gray-800 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors z-10">
            <i class="bi bi-chevron-right text-lg"></i>
        </button>

        <!-- Product Image -->
        <img src="/product.png" alt="Navy Trousers" class="w-full h-full object-contain mix-blend-multiply dark:mix-blend-normal">
    </div>

    <!-- Pagination Dots -->
    <div class="mt-8 flex items-center justify-center gap-2">
        <div class="w-2 h-2 rounded-full bg-gray-900 dark:bg-white"></div>
        <div class="w-2 h-2 rounded-full bg-gray-300 dark:bg-gray-600"></div>
        <div class="w-2 h-2 rounded-full bg-gray-300 dark:bg-gray-600"></div>
    </div>
</div>
```